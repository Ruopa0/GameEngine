[package]
name = "test_ron"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.14.0", default-features = false }
ron = "0.8"
serde = { version = "1.0", features = ["derive"] }


[workspace]

